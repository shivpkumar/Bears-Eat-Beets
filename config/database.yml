mysql: &mysql
  encoding: utf8
  adapter: mysql2
  username: root
  host: localhost

common: &defaults
  <<: *mysql

development:
  <<: *defaults
  database: beb_development

test:
  <<: *defaults
  database: beb_test

production:
  <<: *defaults
  database: beb_production
